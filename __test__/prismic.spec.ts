import prismicClient from "../src/services/prismic";

describe("Prismic", () => {
  it("should be client ", async () => {
    //const res = await prismicClient().getByType("portfolio-custom-type");
    //expect(typeof res.results).toBe("object");
  });
});
